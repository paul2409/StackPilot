apiVersion: v1
kind: Service
metadata:
  name: mock-exchange
  namespace: stackpilot
spec:
  selector:
    app: mock-exchange
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP