apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.postgresIdentity.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.postgresIdentity.name }}
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP