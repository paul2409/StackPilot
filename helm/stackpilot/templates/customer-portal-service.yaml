apiVersion: v1
kind: Service
metadata:
  name: customer-portal
  namespace: {{ .Release.Namespace }}
  labels:
    app: customer-portal
    {{- include "stackpilot.commonLabels" . | nindent 4 }}
spec:
  selector:
    app: customer-portal
    stackpilot.io/environment: {{ .Values.global.environment }}
  ports:
    - port: {{ .Values.customerPortal.service.port }}
      targetPort: {{ .Values.customerPortal.service.port }}
      protocol: TCP
  type: ClusterIP
