apiVersion: apps/v1
kind: Deployment
metadata:
  name: system-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "stackpilot.commonLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.systemService.replicaCount }}
  selector:
    matchLabels:
      app: system-service
      stackpilot.io/environment: {{ .Values.global.environment }}
  template:
    metadata:
      labels:
        app: system-service
        {{- include "stackpilot.commonLabels" . | nindent 8 }}
    spec:
      containers:
        - name: system-service
          image: "{{ .Values.systemService.image.repository }}:{{ .Values.systemService.image.tag }}"
          imagePullPolicy: {{ .Values.systemService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.systemService.service.port }}
          env:
            - name: ENV
              value: {{ .Values.global.environment | quote }}
            - name: IDENTITY_SERVICE_URL
              value: "http://identity-service:{{ .Values.identityService.service.port }}"
            - name: WALLET_SERVICE_URL
              value: "http://wallet-service:{{ .Values.walletService.service.port }}"
          readinessProbe:
            httpGet:
              path: {{ .Values.systemService.probes.readinessPath }}
              port: {{ .Values.systemService.service.port }}
          livenessProbe:
            httpGet:
              path: {{ .Values.systemService.probes.livenessPath }}
              port: {{ .Values.systemService.service.port }}
          resources:
            requests:
              cpu: {{ .Values.resources.default.requests.cpu }}
              memory: {{ .Values.resources.default.requests.memory }}
            limits:
              cpu: {{ .Values.resources.default.limits.cpu }}
              memory: {{ .Values.resources.default.limits.memory }}