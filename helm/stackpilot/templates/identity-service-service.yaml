apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.identity.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.identity.name }}
  ports:
    - port: 80
      targetPort: {{ .Values.identity.port }}
  type: ClusterIP