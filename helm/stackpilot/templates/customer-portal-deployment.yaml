apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-portal
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "stackpilot.commonLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.customerPortal.replicaCount }}
  selector:
    matchLabels:
      app: customer-portal
      stackpilot.io/environment: {{ .Values.global.environment }}
  template:
    metadata:
      labels:
        app: customer-portal
        {{- include "stackpilot.commonLabels" . | nindent 8 }}
    spec:
      containers:
        - name: customer-portal
          image: "{{ .Values.customerPortal.image.repository }}:{{ .Values.customerPortal.image.tag }}"
          imagePullPolicy: {{ .Values.customerPortal.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.customerPortal.service.port }}
          env:
            - name: ENV
              value: {{ .Values.global.environment | quote }}
            - name: PORT
              value: {{ .Values.customerPortal.service.port | quote }}
            - name: IDENTITY_SERVICE_URL
              value: "http://identity-service:{{ .Values.identityService.service.port }}"
            - name: WALLET_SERVICE_URL
              value: "http://wallet-service:{{ .Values.walletService.service.port }}"
            - name: SYSTEM_SERVICE_URL
              value: "http://system-service:{{ .Values.systemService.service.port }}"
          readinessProbe:
            httpGet:
              path: {{ .Values.customerPortal.probes.readinessPath }}
              port: {{ .Values.customerPortal.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: {{ .Values.customerPortal.probes.livenessPath }}
              port: {{ .Values.customerPortal.service.port }}
            initialDelaySeconds: 15
            periodSeconds: 15
            failureThreshold: 6
          resources:
            requests:
              cpu: {{ .Values.resources.default.requests.cpu }}
              memory: {{ .Values.resources.default.requests.memory }}
            limits:
              cpu: {{ .Values.resources.default.limits.cpu }}
              memory: {{ .Values.resources.default.limits.memory }}
