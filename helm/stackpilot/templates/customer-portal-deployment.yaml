apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.customerPortal.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.customerPortal.name }}
spec:
  replicas: {{ .Values.customerPortal.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.customerPortal.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.customerPortal.name }}
    spec:
      containers:
        - name: {{ .Values.customerPortal.name }}
          image: {{ .Values.image.registry }}/customer-portal:{{ .Values.customerPortal.tag }}
          ports:
            - containerPort: {{ .Values.customerPortal.port }}
          env:
            - name: SERVICE_NAME
              value: {{ .Values.customerPortal.name }}
            - name: SERVICE_VERSION
              value: "1.0.0"
            - name: ENV
              value: "dev"
            - name: PORT
              value: "{{ .Values.customerPortal.port }}"
            - name: IDENTITY_SERVICE_URL
              value: "http://{{ .Values.identity.name }}"
            - name: WALLET_SERVICE_URL
              value: "http://{{ .Values.wallet.name }}"
            - name: SYSTEM_SERVICE_URL
              value: "http://{{ .Values.system.name }}"
            - name: REQUEST_TIMEOUT
              value: "2.0"

          readinessProbe:
            httpGet:
              path: /ready
              port: {{ .Values.customerPortal.port }}
            initialDelaySeconds: 5
            periodSeconds: 5

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.customerPortal.port }}
            initialDelaySeconds: 10
            periodSeconds: 10